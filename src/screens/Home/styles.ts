import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white'
  },
  pokeballImage: {
    width: 300,
    height: 300,
    position: 'absolute',
    right: -80,
    top: -80,
    opacity: 0.25
  }
});
